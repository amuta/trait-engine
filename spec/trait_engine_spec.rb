# frozen_string_literal: true

RSpec.describe TraitEngine do
  it "has a version number" do
    expect(TraitEngine::VERSION).not_to be nil
  end

  it "does something useful" do
    # expect(false).to eq(true)
  end
end
